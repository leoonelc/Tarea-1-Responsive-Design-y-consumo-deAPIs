{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "SIGAT FLODECOL (localhost)",
            "url": "http://localhost/sigat_flodecol/",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
